## 安全策略（SECURITY）

### 支持范围
此项目目前以本地开发/自托管使用为主。欢迎反馈安全问题，我们会尽快响应与修复。

### 如何报告漏洞
请避免在公开 Issue 里直接披露可复现的攻击细节。

建议包含以下信息：
- 漏洞描述与影响范围（版本/模块/接口）
- 复现步骤（尽量最小化样例）
- 预期行为 vs 实际行为
- 建议修复方向（可选）

### 安全注意事项（默认行为）
- 数据库查询接口仅允许 `SELECT/CTE`，并拦截多语句与危险关键字
- 开发环境下 Vite dev server 默认绑定 `127.0.0.1`，请不要暴露到不可信网络
- 请勿提交真实数据、`.env`、`uploads/`、`backend/data/` 等运行时文件到公开仓库

